import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({

    info: {
        display:'flex',
        flexDirection:'column',
        marginTop:50,
    },
    location: {
        display:'flex',
        flexDirection:'column',
        marginTop:15
    }
}))