import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(() =>({
    root: {
        marginTop: 60,
        marginLeft:30,
        marginRight:30
    },
    kernel: {
        display:'flex',
        flexDirection:'row'
    }
}));
